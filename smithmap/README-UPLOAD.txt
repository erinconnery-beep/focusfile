SMITH RIVER RANCH — FOCUSFILE TEST PACKAGE

Target address:
https://www.focusfile.org/smithmap/

GitHub Pages setup:
1. Extract this ZIP.
2. Upload every extracted file and folder to the root of the GitHub repository used for "smithmap".
3. Keep the folder names and structure unchanged.
4. In the repository Settings, open Pages and publish from the branch containing these files.

The package uses portable relative links, so the website, interactive map, imagery, fonts, and GeoJSON map layers will work beneath the /smithmap/ path.

Website password: SR1

The satellite and topographic map backgrounds are loaded from Esri when the visitor opens the map, so an internet connection is required for those backgrounds.
